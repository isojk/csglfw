# Build x64
dotnet build -c Release -p:Platform="x64"

# Build ARM64
dotnet build -c Release -p:Platform="ARM64"

# Pack x64
dotnet pack -c Release -p:Platform=x64 -o <OUTPUT>

# Pack ARM64
dotnet pack -c Release -p:Platform=ARM64 -o <OUTPUT>
